<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <!-- EditText para o usuário digitar o nome -->
    <EditText
        android:id="@+id/edtNome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Digite seu nome"
        android:layout_marginBottom="16dp"/>

    <!-- Botão para gerar CheckBoxes -->
    <Button
        android:id="@+id/btnGerarCheckBoxes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Gerar CheckBoxes"
        android:layout_marginBottom="16dp"/>

    <!-- LinearLayout onde as CheckBoxes serão geradas dinamicamente -->
    <LinearLayout
        android:id="@+id/llCheckBoxes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"/>

</LinearLayout>
